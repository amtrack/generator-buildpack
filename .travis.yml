language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-buildpack' ]; then cd .. && eval "mv $currentfolder generator-buildpack" && cd generator-buildpack; fi

